[Unit]
Description=Cloudflare Tunnel Daemon for Vishwaguru Billing Remote Access
After=network.target

[Service]
TimeoutStartSec=0
Type=notify
ExecStart=/usr/local/bin/cloudflared tunnel run --token YOUR_CLOUDFLARE_TUNNEL_TOKEN
Restart=on-failure
RestartSec=5s

[Install]
WantedBy=multi-user.target
